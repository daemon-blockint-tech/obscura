{
  "name": "obscura",
  "private": true,
  "workspaces": [
    "web"
  ],
  "scripts": {
    "build:program": "anchor build",
    "test:program": "anchor test",
    "dev:web": "yarn workspace web dev",
    "build:web": "yarn workspace web build",
    "test": "yarn workspace web test && anchor test"
  },
  "devDependencies": {
    "@coral-xyz/anchor": "^0.30.1",
    "@solana/web3.js": "^1.91.0",
    "ts-mocha": "^10.0.0",
    "mocha": "^10.4.0",
    "chai": "^4.4.0",
    "typescript": "^5.4.0"
  }
}
